# Archived audio implementation

The active app now builds the AAC microphone and RTP packetizer again. This folder
is retained as the source archive used to restore that integration and is excluded
from the build. The active copies are in `FaceTrackCam/`.
